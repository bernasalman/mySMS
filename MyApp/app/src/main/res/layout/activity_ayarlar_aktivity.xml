<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AyarlarAktivity">

    <include

        android:id="@+id/ayarlar_toolbar"
        layout="@layout/app_bar_layout"

        />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profil_resmi_ayarla"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="35dp"
        android:src="@drawable/ic_person"
        app:civ_border_color="@color/purple_500"
        app:civ_border_width="4dp"
        />

    <EditText
        android:id="@+id/kullanici_adi_ayarla"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_below="@id/profil_resmi_ayarla"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="10dp"
        android:layout_marginRight="15dp"
        android:background="@drawable/girdiler"
        android:drawableStart="@drawable/ic_baseline_person_24"
        android:hint="Kullanıcı Adi"
        android:inputType="textMultiLine"
        android:padding="4dp"
        android:textAlignment="center"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/profil_durum_ayarla"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="10dp"
        android:layout_below="@id/kullanici_adi_ayarla"
        android:inputType="textMultiLine"
        android:hint="Kullanıcı Durumu"
        android:textAlignment="center"
        android:drawableStart="@drawable/ic_baseline_event_note_24"
        android:textStyle="bold"
        android:background="@drawable/girdiler"
        android:layout_marginRight="15dp"
        android:layout_marginLeft="15dp"
        android:padding="4dp"
        />

    <Button
        android:id="@+id/ayarlari_guncelle_button"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="10dp"
        android:layout_below="@id/profil_durum_ayarla"
        android:inputType="textMultiLine"
        android:text="Güncelle"
        android:textSize="24sp"
        android:textAlignment="center"
        android:textStyle="bold"
        android:background="@drawable/buttonlar"
        android:textColor="@android:color/background_light"
        android:layout_marginRight="15dp"
        android:layout_marginLeft="15dp"
        android:padding="4dp"
        android:textAllCaps="false"
        />
</RelativeLayout>